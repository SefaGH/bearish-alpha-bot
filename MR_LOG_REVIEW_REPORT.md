# Mean Reversion (MR) Log Review — End-to-End Health + P&L Price Feed Integrity

Evidence source:
- `logs/live_trading_20251227_220109_746903.log`

Scope:
- Strategy: `mean_reversion` (VWAP Mean Reversion)
- Symbol: `BTC/USDT:USDT`
- Signal timeframe: `5m`

---

## 1) Executed MR signals (proof of end-to-end execution)

Two MR signals were fully enqueued, bridged into the engine, executed, and resulted in opened positions.

| Signal ID | Time (log ts) | Side | Quality score | Resulting position |
|---|---:|---|---:|---|
| `mean_reversion_BTC_USDT_USDT_20251227_221621_925582` | 2025-12-27 22:16:21 | sell | 0.5984 | `pos_BTC/USDT:USDT_1766873782` |
| `mean_reversion_BTC_USDT_USDT_20251227_235127_966142` | 2025-12-27 23:51:27 | sell | 0.5808 | `pos_BTC/USDT:USDT_1766879488` |

Key pipeline markers observed for both:
- Coordinator emits `SIGNAL_BREAKDOWN` + `ENQUEUED`
- Engine receives bridge messages: `[BRIDGE-RECEIVE]` then `[BRIDGE-TRANSFER]`
- Engine registers/opens the position and begins emitting `[P&L-UPDATE]`

---

## 2) Market-data provenance at entry moments (trigger price)

At both entry moments, trigger diagnostics show the same path:
- `requested_source=mid`
- `resolved_source=mid`
- `ticker_age_ms≈329`
- `reason_tags=[none]`

This suggests a *fresh* ticker at the moment the MR signals were generated and accepted.

---

## 3) P&L “current price staleness” (run-length analysis)

Method:
- Parse `[P&L-UPDATE]` lines for each position.
- Compute run-lengths of identical `Current` prices across consecutive updates.

Results were generated by:
- `scripts/pnl_staleness_report.py`
- Output artifact: `logs/pnl_staleness_report.txt`

### Position: `pos_BTC/USDT:USDT_1766873782`
- updates=143, median update cadence ≈ 10s
- run-lengths: median=6, p95=7, max=7
- longest identical-current runs are ~60 seconds (7 ticks at 10s cadence)

Interpretation:
- Mild “sticky current” consistent with rounding/slow moves; nothing extreme.

### Position: `pos_BTC/USDT:USDT_1766879488`
- updates=1855, median update cadence ≈ 10s
- run-lengths: median=6, p95=11, max=23
- worst window: `current=87680.20` repeated 23 times (~221 seconds)

Interpretation:
- There are multi-minute windows where the reported `Current` price does not change.
- During the worst window, `[TRIGGER-DIAG]` continues to report low `ticker_age_ms` (≈326–354ms) and *unchanged* bid/ask/last values (`bid=87680.00 ask=87680.20`), which aligns with the repeated P&L `Current=87680.20`.
- This points to “market is flat at the printed precision / spread is stable” rather than a stale price feed.

---

## 4) Preliminary verdicts

- Signal → Coordinator → Engine → Position opening: **PASS** (evidence of two successful executions)
- Entry-time trigger diagnostics: **PASS** (fresh ticker at signal generation time)
- P&L “Current” freshness over long holds: **PASS (tentative)** (multi-minute identical-current streaks are accompanied by fresh tickers and unchanged bid/ask in `[TRIGGER-DIAG]`)

---

## 5) Next evidence to extract (to confirm or rule out a real staleness bug)

If you still suspect P&L feed issues, the most decisive checks are:
1) Confirm which price is used for P&L per side (e.g., short uses `ask` to simulate buy-to-close slippage vs `mid`/`mark`).
2) For any suspicious window, compare P&L `Current` to `[TRIGGER-DIAG]` `bid/ask/last` values at the same timestamps.
3) If `[TRIGGER-DIAG]` moves but P&L `Current` does not, then it’s a genuine staleness/price-source bug.
